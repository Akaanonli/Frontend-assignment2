import React from "react";
import { Link } from "react-router-dom";
/*import { RestClient } from "./RestClient"

function Environments() {

	let [environments, setEnvironments] = React.useState<Array<any>>([])

	React.useEffect(() => {
		RestClient.getEnvironments()
		          .then(environments => setEnvironments(environments))
	}, [])

	return (
		<div>
			<h1>Environments</h1>
			{environments.map((env: any, i: number) =>
				<Link key={i} className='blockLink' to={`environment/${env.id}`}>{env.place}</Link>
			)}
		</div>
    )
}
export default Environments;
*/
